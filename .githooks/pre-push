#!/bin/sh

branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ]; then
  echo "Direct pushes from main are blocked in this repository."
  echo "Create a feature branch, push that branch, and open a pull request instead."
  exit 1
fi
